#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: 0 is a success
 */
int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
