#include <stdio.h>
/**
 * main - a program that prints alphabets in lowercase
 * Return: 0 (Is a success)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
		putchar(c, c++);
	}
	putchar('\n');
	return (0);
}
