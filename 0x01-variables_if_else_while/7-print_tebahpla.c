#include <stdio.h>
/**
 * main - a program that prints alphabets in reverse
 * Return: 0 is a success
 */
int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
